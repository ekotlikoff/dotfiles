set hidden " Possibility to have more than one unsaved buffers.
set path+=** " Search recursively search through subdirectories
" List buffers and prepare to open one.
nnoremap <leader>l :ls<CR>:b<space>
