set-option -g prefix C-a
unbind-key C-b

# add double-tap meta key to toggle last window
bind-key C-a last-window
bind-key r source-file ~/.tmux.conf\; display-message '.tmux.conf reloaded'

source-file $DOT/tmux/config/*
